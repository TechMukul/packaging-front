/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['via.placeholder.com', 'woxnpackaging.appspot.com','firebasestorage.googleapis.com'],
  },
};

export default nextConfig;
